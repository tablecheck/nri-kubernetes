name: com.newrelic.kubernetes
description: New Relic Infrastructure’s Integration for collecting metrics for your Kubernetes objects.
protocol_version: 2
os: linux

commands:
  metrics:
    command:
      - ./bin/nr-kubernetes
      - --metrics
    interval: 15
